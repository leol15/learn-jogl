package com.play.app.mesh;

import java.nio.FloatBuffer;

import com.play.app.graphics.VAO;
import com.play.app.utils.Func;

import org.lwjgl.BufferUtils;

import org.lwjgl.opengl.GL32.*;

/**
 * vertex layout
 */
public class TiledFloor {

    // public static VAO createFloor(int w, int h) {

    // vboVertices = glGenBuffers();
    // glBindBuffer(GL_ARRAY_BUFFER, vboVertices);

    // vboIndices = glGenBuffers();
    // glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, vboIndices);

    // final FloatBuffer verticies = BufferUtils.createFloatBuffer(w * h * )
    // return vao;
    // }

}
